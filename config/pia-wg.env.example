# PIA WireGuard Environment Variables
# Location: /etc/pia-wg.env
#
# This file stores PIA credentials and server information for port forwarding.
# Run pia-wireguard-bootstrap.sh to generate these values.
#
# SECURITY: After creating this file, set permissions:
#   chmod 600 /etc/pia-wg.env
#   chown root:root /etc/pia-wg.env

# Your PIA username (starts with 'p' followed by numbers)
PIA_USER='p1234567'

# Your PIA password
PIA_PASS='your_pia_password_here'

# PIA region ID (e.g., us_seattle, uk_london, de_frankfurt)
# Must be a region that supports port forwarding
PIA_REGION='us_seattle'

# PIA WireGuard server common name (from bootstrap script)
PIA_WG_CN='wg-server.privateinternetaccess.com'

# PIA WireGuard server IP address (from bootstrap script)
PIA_WG_IP='1.2.3.4'

# PIA Meta server common name (from bootstrap script)
PIA_META_CN='meta-server.privateinternetaccess.com'

# PIA Meta server IP address (from bootstrap script)
PIA_META_IP='5.6.7.8'

# PIA server VIP for port forwarding (from bootstrap script)
PIA_SERVER_VIP='10.0.0.1'

# Your WireGuard private key (from bootstrap script)
WG_PRIVKEY='your_wireguard_private_key_here'

# Your WireGuard public key (from bootstrap script)
WG_PUBKEY='your_wireguard_public_key_here'
